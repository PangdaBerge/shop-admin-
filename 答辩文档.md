# 🛒 商城后台管理系统 —— Vue 课程设计答辩文档

> **适用场景：** 大三 Vue 课程设计大作业答辩  
> **组员人数：** 2 人  
> **目标：** 两人答辩得分一致，均有充分的讲解内容

---

## 目录

- [一、项目概述](#一项目概述)
- [二、技术栈](#二技术栈)
- [三、系统架构](#三系统架构)
- [四、数据库设计](#四数据库设计)
- [五、后端 API 设计](#五后端-api-设计)
- [六、前端核心原理](#六前端核心原理)
- [七、功能模块详解](#七功能模块详解)
- [八、答辩分工方案](#八答辩分工方案)
- [九、答辩演示流程](#九答辩演示流程)
- [十、常见 Q&A 准备](#十常见-qa-准备)
- [附录：关键代码对照表](#附录关键代码对照表)

---

## 一、项目概述

### 1.1 项目简介

本项目是一个**全栈商城后台管理系统**，采用前后端分离架构。前端使用 **Vue 3 + Vite** 构建 SPA 单页应用，后端使用 **Python FastAPI** 提供 RESTful API，数据库使用 **SQLite** 进行数据持久化。

### 1.2 核心功能模块

| 模块 | 功能描述 | 对应文件 |
|------|----------|----------|
| 🔐 用户认证 | 登录/登出、角色区分(admin/user)、路由权限守卫 | `Login.vue`、`store/user.js`、`router/index.js` |
| 🛍️ 商品管理 | 商品列表展示、搜索过滤、分类筛选、上架/下架 | `Dashboard.vue`、`ProductList.vue` |
| 📂 分类筛选 | 按商品类别过滤、动态加载分类、管理员新建分类 | `Dashboard.vue`、`main.py` |
| 🛒 购物车 | 加入购物车、数量累加、移除、总价计算、结算付款 | `store/cart.js`、`Dashboard.vue` |
| 🧾 订单系统 | 创建订单、管理员全站订单监控、用户查看历史订单 | `Dashboard.vue`、`main.py` |
| 📋 任务看板 | Kanban 三列看板、新建/编辑/删除/移动任务、优先级标签 | `TaskBoard.vue`、`main.py` |
| 🌙 深色模式 | 一键切换浅/深色主题、跟随系统偏好、localStorage 持久化 | `store/theme.js`、`ThemeToggle.vue` |
| 📱 响应式设计 | 手机/平板/桌面三端适配、侧边栏自适应 | `App.vue`、`Sidebar.vue` |
| 💾 数据持久化 | SQLite 数据库存储、localStorage 存储主题和会话 | `init_db.py`、`main.py` |

---

## 二、技术栈

### 2.1 前端技术

| 技术 | 版本 | 用途 | 为什么选它 |
|------|------|------|------------|
| **Vue 3** | 3.5 | 前端框架 | Composition API 写法更灵活，逻辑复用性强 |
| **Vite** | 8.0 | 构建工具 | 基于 ESM 的极速开发服务器，HMR 热更新 |
| **Pinia** | 3.0 | 状态管理 | Vue 官方推荐，TypeScript 友好，模块化设计 |
| **Vue Router** | 4.6 | 路由管理 | SPA 页面跳转，支持路由懒加载和导航守卫 |
| **Tailwind CSS** | 4.3 | 样式框架 | 原子化 CSS，内置暗色模式支持，响应式断点 |
| **Axios** | 1.17 | HTTP 客户端 | 发送 API 请求，支持拦截器和请求头配置 |

### 2.2 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| **FastAPI** | latest | Python 异步 Web 框架，自动生成 API 文档 |
| **SQLite 3** | built-in | 轻量级关系型数据库，无需安装 |
| **Pydantic** | built-in | 数据校验，请求/响应模型定义 |
| **CORS 中间件** | built-in | 解决前后端跨域问题 |

---

## 三、系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         浏览器 (Browser)                         │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Vue 3 SPA (单页应用)                          │  │
│  │  ┌─────────┐  ┌──────────┐  ┌────────────────────────┐   │  │
│  │  │  Views   │  │  Store   │  │  Components            │   │  │
│  │  │ Login    │  │  user.js │  │  Sidebar.vue           │   │  │
│  │  │ Dashboard│  │  cart.js │  │  ProductList.vue       │   │  │
│  │  │ TaskBoard│  │  theme.js│  │  ThemeToggle.vue       │   │  │
│  │  └────┬─────┘  └────┬─────┘  └────────────────────────┘   │  │
│  │       │              │                                      │  │
│  │  ┌────┴──────────────┴─────┐                                │  │
│  │  │   Vue Router (路由守卫)  │  ← beforeEach 权限验证        │  │
│  │  └────────────┬────────────┘                                │  │
│  └───────────────┼─────────────────────────────────────────────┘  │
└──────────────────┼────────────────────────────────────────────────┘
                   │  HTTP (Axios)
                   │  Authorization: {token}
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│              FastAPI Server (Port 3000)                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ /api/    │  │ /api/    │  │ /api/    │  │ /api/    │       │
│  │ login    │  │ products │  │ orders   │  │ tasks    │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
│       └──────────────┴──────────────┴──────────────┘            │
│                          │                                       │
│                    SQLite 操作                                    │
└──────────────────────────┼──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SQLite 数据库 (shop_system.db)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  users   │  │ products │  │  orders  │  │  tasks   │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 前端组件树

```
App.vue (根组件)
├── Login 页面 (无需侧边栏)
│   └── ThemeToggle.vue
│
└── 主布局 (含侧边栏)
    ├── Sidebar.vue          ← 侧边栏导航
    ├── 顶栏
    │   ├── 汉堡菜单按钮      ← 移动端
    │   └── ThemeToggle.vue  ← 深色模式切换
    │
    └── <router-view>        ← 内容区
        ├── Dashboard.vue     ← 控制台 (商品 + 订单)
        │   └── ProductList.vue
        └── TaskBoard.vue     ← 任务看板
```

### 3.3 数据流图 (以"用户登录→浏览商品→加入购物车→结算"为例)

```
用户输入账号密码
    │
    ▼
Login.vue ──(axios POST)──▶ /api/login
    │                           │
    │                           ▼
    │                      FastAPI 查询 users 表
    │                           │
    │                           ▼
    │                      返回 {token, userInfo}
    │                           │
    ▼                           │
userStore.login() ◀─────────────┘
    │
    │ 存储 token 到 localStorage
    │ 存储 userInfo 到 localStorage
    │
    ▼
router.push('/dashboard')  ← 路由跳转
    │
    ▼
Dashboard.vue onMounted()
    │
    ├──(axios GET)──▶ /api/products ──▶ SQL: SELECT * FROM products
    │                      │
    │                      ▼
    │                  products.value = [...]  ← 响应式数据
    │                      │
    │                      ▼
    │                 ProductList.vue 渲染商品表格
    │
    └──(axios GET)──▶ /api/orders ──▶ SQL: SELECT * FROM orders
                           │
                           ▼
                      orders.value = [...]  ← 响应式数据
                           │
                           ▼
                      渲染订单表格

用户点击「加入购物车」
    │
    ▼
cartStore.addToCart(product)
    │
    │  items.value 更新 (响应式)
    │  totalPrice 自动重新计算 (computed)
    │
    ▼
购物车 UI 自动更新 (Vue 响应式系统)

用户点击「立即结算付款」
    │
    ▼
checkout() ──(axios POST)──▶ /api/orders {total_price, items_detail}
                                  │
                                  ▼
                             SQL: INSERT INTO orders
                                  │
                                  ▼
                             cartStore.clearCart()
                             fetchOrders() ← 刷新订单列表
```

---

## 四、数据库设计

### 4.1 ER 图 (实体关系)

```
┌──────────────────┐       ┌──────────────────┐
│     users        │       │    products      │
├──────────────────┤       ├──────────────────┤
│ id (PK)          │       │ id (PK)          │
│ username (UNIQUE)│       │ name             │
│ password         │       │ price            │
│ role             │       │ category         │
└──────────────────┘       └──────────────────┘

┌──────────────────┐       ┌──────────────────┐
│     orders       │       │     tasks        │
├──────────────────┤       ├──────────────────┤
│ id (PK)          │       │ id (PK)          │
│ username (FK)    │───┐   │ title            │
│ total_price      │   │   │ description      │
│ items_detail     │   │   │ status           │
│ created_at       │   │   │ priority         │
└──────────────────┘   │   │ assignee         │
                        │   │ created_at       │
                        │   └──────────────────┘
                        │
         users.username ┘ (逻辑外键，不是数据库外键)
```

### 4.2 表结构详解

#### users 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用户 ID |
| username | TEXT | NOT NULL UNIQUE | 用户名 |
| password | TEXT | NOT NULL | 密码 |
| role | TEXT | NOT NULL DEFAULT 'user' | 角色：'admin' 或 'user' |

#### products 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 商品 ID |
| name | TEXT | NOT NULL | 商品名称 |
| price | REAL | NOT NULL | 商品价格 |
| category | TEXT | NOT NULL DEFAULT '未分类' | 商品分类 |

#### orders 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 订单号 |
| username | TEXT | NOT NULL | 购买用户名 |
| total_price | REAL | NOT NULL | 支付总额 |
| items_detail | TEXT | NOT NULL | 购买明细 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 交易时间 |

#### tasks 表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 任务 ID |
| title | TEXT | NOT NULL | 任务标题 |
| description | TEXT | DEFAULT '' | 任务描述 |
| status | TEXT | NOT NULL DEFAULT 'todo' | 状态：'todo'/'in_progress'/'done' |
| priority | TEXT | NOT NULL DEFAULT 'medium' | 优先级：'high'/'medium'/'low' |
| assignee | TEXT | DEFAULT '' | 负责人 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 关键 SQL 语句

```sql
-- 查询所有商品 (按分类筛选)
SELECT * FROM products WHERE category = ? ORDER BY id ASC;

-- 获取不重复的商品分类
SELECT DISTINCT category FROM products ORDER BY category;

-- 管理员查看所有订单
SELECT * FROM orders ORDER BY id DESC;

-- 普通用户只看自己的订单 (权限隔离)
SELECT * FROM orders WHERE username = ? ORDER BY id DESC;

-- 更新任务状态 (看板拖拽)
UPDATE tasks SET status = ? WHERE id = ?;
```

---

## 五、后端 API 设计

### 5.1 API 一览表

| 方法 | 路径 | 权限 | 功能 | 请求体 |
|------|------|------|------|--------|
| POST | `/api/login` | 无 | 用户登录 | `{username, password}` |
| GET | `/api/products` | 无 | 获取商品列表 | 可选参数 `?category=xxx` |
| GET | `/api/categories` | 无 | 获取所有分类 | 无 |
| POST | `/api/products` | admin | 上架商品 | `{name, price, category}` |
| DELETE | `/api/products/{id}` | admin | 下架商品 | 无 |
| POST | `/api/orders` | 登录 | 创建订单 | `{total_price, items_detail}` |
| GET | `/api/orders` | 登录 | 获取订单列表 | 无 |
| GET | `/api/tasks` | 无 | 获取任务列表 | 可选参数 `?status=xxx` |
| POST | `/api/tasks` | 登录 | 创建任务 | `{title, description, status, priority, assignee}` |
| PUT | `/api/tasks/{id}` | 登录 | 更新任务 | 可选字段 |
| DELETE | `/api/tasks/{id}` | 登录 | 删除任务 | 无 |

### 5.2 请求/响应格式

所有 API 统一返回 JSON 格式：

```json
// 成功响应
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}

// 失败响应
{
  "code": 401,
  "message": "用户名或密码错误！"
}
```

### 5.3 权限控制原理

```
客户端请求头: Authorization: "zff-user" 或 "admin-admin"

后端解析:
  username = token.split('-')[0]   → "zff"
  role     = token.split('-')[1]   → "user"

管理员操作 (上架/下架):
  if not authorization.endswith('-admin'):
      raise HTTPException(403, "权限不足")

订单查看权限:
  admin → SELECT * FROM orders          (看全部)
  user  → SELECT * WHERE username = ?   (只看自己)
```

---

## 六、前端核心原理

### 6.1 Vue 3 Composition API (`<script setup>`)

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue'

// ref: 创建响应式变量 (基本类型)
const searchKey = ref('')
const products = ref([])

// computed: 计算属性 (依赖变化时自动重新计算)
const filteredProducts = computed(() => {
  return products.value.filter(p =>
    p.name.toLowerCase().includes(searchKey.value.toLowerCase())
  )
})

// watch: 监听变化并执行副作用
watch(selectedCategory, () => {
  fetchProducts()  // 分类切换时自动重新请求
})

// onMounted: 组件挂载后执行
onMounted(() => {
  fetchProducts()
  fetchCategories()
  fetchOrders()
})
</script>
```

**核心概念对比 (Options API vs Composition API)**

| | Options API (Vue 2 风格) | Composition API (Vue 3) |
|------|---------------------------|--------------------------|
| 数据 | `data() { return { count: 0 } }` | `const count = ref(0)` |
| 计算 | `computed: { double(){} }` | `const double = computed(() => ...)` |
| 方法 | `methods: { increment(){} }` | `const increment = () => { ... }` |
| 生命周期 | `mounted(){}` | `onMounted(() => { ... })` |
| 优点 | 按选项分类 | **按功能聚合**，逻辑复用更容易 |

我们选择 Composition API 的理由：可以把同一功能的数据、计算、方法写在一起，而不是分散在不同的选项中。

### 6.2 Vue 响应式原理 (简化版)

```
┌──────────────────────────────────────────────────────┐
│  Vue 3 使用 Proxy 实现响应式                          │
│                                                      │
│  ref(0) 创建的变量:                                   │
│    ├── 当 .value 被读取时 → 收集依赖 (track)          │
│    └── 当 .value 被修改时 → 触发更新 (trigger)        │
│                                                      │
│  computed 计算属性:                                   │
│    ├── 内部维护一个依赖列表                            │
│    ├── 依赖变化 → 标记为脏 (dirty)                    │
│    └── 下次访问 → 重新计算并缓存结果                   │
│                                                      │
│  watch 侦听器:                                        │
│    └── 指定的 ref 变化 → 执行回调函数                  │
│                                                      │
│  整个流程保证: 数据变化 → UI 自动更新                  │
│  开发者不需要手动操作 DOM                              │
└──────────────────────────────────────────────────────┘
```

**以购物车为例：**

```
用户点击「加入购物车」
  → cartStore.addToCart(product)
  → items.value.push({...product, quantity: 1})
  → Vue Proxy 检测到 items 数组变化
  → 触发 totalPrice (computed) 重新计算
  → 触发模板中 {{ cartStore.totalPrice }} 重新渲染
  → 用户看到总价自动更新 ✨
```

### 6.3 Pinia 状态管理

本项目有 3 个 Store，采用组合式 Store (Setup Store) 写法：

| Store | 文件 | 管理的状态 | 核心方法 |
|-------|------|-----------|----------|
| `useUserStore` | `store/user.js` | token、userInfo | login()、logout() |
| `useCartStore` | `store/cart.js` | items[]、totalPrice | addToCart()、removeFromCart()、clearCart() |
| `useThemeStore` | `store/theme.js` | isDark | toggle() |

**userStore 详解：**

```js
// store/user.js —— 用户认证状态管理
export const useUserStore = defineStore('user', () => {
  // 初始化时从 localStorage 恢复登录状态 (防止刷新后丢失)
  const token = ref(localStorage.getItem('token') || '')
  const userInfo = ref(JSON.parse(localStorage.getItem('userInfo') || '{}'))

  // watch: token 变化时自动同步到 localStorage
  watch(token, (newVal) => {
    if (newVal) localStorage.setItem('token', newVal)
    else localStorage.removeItem('token')
  })

  // 登录：调用后端 API，成功后更新状态
  const login = async (username, password) => {
    const res = await axios.post('http://localhost:3000/api/login',
      { username, password })
    if (res.data.code === 200) {
      token.value = res.data.data.token
      userInfo.value = res.data.data.user
      localStorage.setItem('userInfo', JSON.stringify(userInfo.value))
      return true
    }
    return false
  }

  const logout = () => {
    token.value = ''
    userInfo.value = {}
    localStorage.removeItem('token')
    localStorage.removeItem('userInfo')
  }

  return { token, userInfo, login, logout }
})
```

### 6.4 Vue Router 路由守卫

```js
// router/index.js —— 全局前置守卫
router.beforeEach((to, from, next) => {
  // ① 设置页面标题
  if (to.meta.title) document.title = to.meta.title

  const userStore = useUserStore()

  // ② 权限检查：需要登录但没 token → 重定向到登录页
  if (to.meta.requiresAuth && !userStore.token) {
    next('/login')
  }
  // ③ 已登录还去登录页 → 拦截并送往控制台
  else if (to.path === '/login' && userStore.token) {
    next('/dashboard')
  }
  // ④ 正常放行
  else {
    next()
  }
})
```

**路由懒加载：** 每个页面组件使用 `() => import('@/views/XXX.vue')` 动态导入，Vite 会自动拆分为独立 chunk，首屏只加载需要的代码。

### 6.5 深色模式原理

```
┌──────────────────────────────────────────────────────────┐
│  深色模式实现 (Tailwind CSS v4 class-based dark mode)     │
│                                                          │
│  1. CSS 配置:                                             │
│     @variant dark (&:where(.dark, .dark *));              │
│     这告诉 Tailwind: 当 HTML 元素有 .dark 类时启用暗色    │
│                                                          │
│  2. Theme Store (theme.js):                               │
│     ├── 读取 localStorage.getItem('theme')                │
│     ├── 若无保存值 → 跟随系统偏好 matchMedia('dark')       │
│     └── watch 监听 isDark → 切换 document 的 .dark 类     │
│                                                          │
│  3. 组件中使用:                                           │
│     class="bg-white dark:bg-gray-800"                     │
│            ↑浅色    ↑暗色                                  │
│     Tailwind 自动根据 .dark 类决定应用哪个样式             │
│                                                          │
│  4. 持久化: localStorage.setItem('theme', 'dark')         │
│     下次打开网页 → 自动恢复主题偏好                        │
└──────────────────────────────────────────────────────────┘
```

### 6.6 响应式设计策略

| 断点 | 屏幕宽度 | 布局调整 |
|------|----------|----------|
| 默认 (手机) | < 768px | 侧边栏隐藏，汉堡菜单呼出；订单用卡片展示；看板单列 |
| md (平板) | ≥ 768px | 看板变为 3 列；表格正常显示 |
| lg (桌面) | ≥ 1024px | 侧边栏固定显示；商品/购物车并排布局 |

**实现方式：**

```html
<!-- 侧边栏：移动端绝对定位+平移隐藏，桌面端固定显示 -->
<aside class="fixed lg:sticky ...
               -translate-x-full lg:translate-x-0">

<!-- 看板列：手机1列，平板3列 -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">

<!-- 表单：手机竖排，平板横排 -->
<div class="flex flex-col md:flex-row gap-3">
```

---

## 七、功能模块详解

### 7.1 登录认证流程

```
┌──────────┐    POST /api/login     ┌──────────┐    SELECT * FROM users    ┌──────────┐
│ Login.vue│ ──────────────────────▶ │ main.py  │ ────────────────────────▶ │  SQLite  │
│          │                         │          │ ◀──────────────────────── │          │
│          │ ◀── {code:200, token} ──│          │    返回用户记录            └──────────┘
└──────┬───┘                         └──────────┘
       │
       ▼
 userStore.login() 返回 true
       │
       ▼
 router.push('/dashboard')
       │
       ▼
 router.beforeEach 守卫检查: token 存在 → 放行
       │
       ▼
 Dashboard.vue onMounted() → fetchProducts() + fetchOrders()
       │
       │  请求头: Authorization: "admin-admin"
       ▼
 后端解析 token → 识别用户角色 → 返回对应数据
```

### 7.2 商品管理 + 分类筛选

**数据流：**
1. `Dashboard.vue` 挂载时调用 `fetchCategories()` 获取分类列表
2. 用户切换分类 → `watch(selectedCategory)` 触发 → `fetchProducts()` 带上 `?category=xxx` 参数
3. 后端 `get_products(category)` 根据参数决定是否加 WHERE 条件
4. 管理员上架时选择分类或输入新分类名 → POST 请求带有 category 字段

**前端实现关键代码：**

```js
// Dashboard.vue <script setup>
const selectedCategory = ref('全部')

// 分类切换时自动重新获取商品
watch(selectedCategory, () => fetchProducts())

const fetchProducts = async () => {
  const params = selectedCategory.value !== '全部'
    ? { category: selectedCategory.value }  // 传给 axios 的 params
    : {}
  const res = await axios.get(`${API}/products`, { params })
  // axios 自动将 params 转为查询字符串: /api/products?category=电子产品
}
```

### 7.3 购物车与结算

**购物车逻辑 (store/cart.js)：**

```js
// 加入购物车
const addToCart = (product) => {
  const existItem = items.value.find(item => item.id === product.id)
  if (existItem) {
    existItem.quantity++           // 已有 → 数量+1
  } else {
    items.value.push({ ...product, quantity: 1 })  // 新商品 → 添加
  }
}

// 总价自动计算 (computed)
const totalPrice = computed(() => {
  return items.value.reduce((sum, item) => sum + item.price * item.quantity, 0)
})
```

**结算流程：**
1. 点击「立即结算付款」
2. 拼接购物车明细：`"商品A x2 | 商品B x1"`
3. POST `/api/orders` 发送 `{total_price, items_detail}`
4. 后端 INSERT INTO orders
5. 清空购物车 `cartStore.clearCart()`
6. 刷新订单列表 `fetchOrders()`

### 7.4 任务看板 (Kanban)

**核心设计：**

```
三列看板 = 一个数据源 + computed 分组

tasks.value = [
  {id:1, title:"...", status:"todo", ...},
  {id:2, title:"...", status:"in_progress", ...},
  {id:3, title:"...", status:"done", ...},
]

const todoTasks = computed(() =>
  tasks.value.filter(t => t.status === 'todo'))

const inProgressTasks = computed(() =>
  tasks.value.filter(t => t.status === 'in_progress'))

const doneTasks = computed(() =>
  tasks.value.filter(t => t.status === 'done'))
```

**移动任务：**

```js
const moveTask = async (task, newStatus) => {
  // 只发送变化的字段 (部分更新)
  await axios.put(`/api/tasks/${task.id}`,
    { status: newStatus },
    { headers: { Authorization: userStore.token } }
  )
  fetchTasks()  // 重新获取所有任务，UI 自动更新
}
```

**新建/编辑弹窗：** 使用同一个表单，通过 `editingTask` 是否为 null 区分新建/编辑模式。

---

## 八、答辩分工方案

### 8.1 分工原则

> **目标：两人得分一致，都能完整回答问题**

- 每人负责独立的讲解模块，互不重叠
- 每人都要能演示**自己负责的功能**
- 交叉提问时，两人都能回答基础问题
- 共用同一份文档，确保知识对齐

### 8.2 推荐分工

#### 👤 组员 A —— 前端架构与 UX 特性（建议先讲）

| 讲解模块 | 时间 | 难度 | 涉及文件 |
|----------|------|------|----------|
| 1. 项目概述 | 1 min | ⭐ | 全部 |
| 2. 前端技术选型 (为什么选 Vue 3/Vite/Pinia/Tailwind) | 2 min | ⭐⭐ | package.json |
| 3. 组件树与页面结构 | 1 min | ⭐⭐ | App.vue, Sidebar.vue |
| 4. Pinia 状态管理 (user/cart/theme) | 3 min | ⭐⭐⭐ | store/*.js |
| **5. 🔥 深色模式实现** | 2 min | ⭐⭐ | store/theme.js, ThemeToggle.vue, style.css |
| **6. 🔥 响应式设计** | 2 min | ⭐⭐ | App.vue, Sidebar.vue, Dashboard.vue |
| 7. **现场演示** | 3 min | — | — |

**组员 A 需掌握的核心问题：**
- Vue 3 Composition API 和 Options API 的区别
- ref 和 reactive 有什么区别
- computed 和 watch 的区别和使用场景
- Pinia 的 defineStore 两种写法
- Tailwind CSS 的 dark: 前缀是怎么工作的
- 响应式布局用了哪些 Tailwind 断点

#### 👤 组员 B —— 后端设计与业务逻辑（建议后讲）

| 讲解模块 | 时间 | 难度 | 涉及文件 |
|----------|------|------|----------|
| 1. 系统架构与数据流 | 2 min | ⭐⭐ | 全部 |
| 2. 数据库设计 (4 张表 + ER 图) | 3 min | ⭐⭐⭐ | init_db.py |
| 3. API 设计与权限控制 | 3 min | ⭐⭐⭐ | main.py |
| 4. 路由守卫 (登录拦截) | 2 min | ⭐⭐ | router/index.js |
| **5. 🔥 分类筛选** | 2 min | ⭐⭐ | Dashboard.vue, main.py |
| **6. 🔥 任务看板 CRUD** | 2 min | ⭐⭐ | TaskBoard.vue, main.py |
| 7. **现场演示** | 3 min | — | — |

**组员 B 需掌握的核心问题：**
- FastAPI 的路由装饰器是怎么工作的
- SQLite 的优缺点
- 前后端分离如何解决跨域问题 (CORS)
- 路由守卫的 beforeEach 三个参数含义
- RESTful API 设计规范
- 项目中权限控制是怎么实现的

### 8.3 知识交叉检查清单

两人**都必须能回答**的基础问题（答辩老师可能随机提问）：

| 问题 | 答案要点 |
|------|----------|
| 这个项目做什么？ | 全栈商城后台管理系统：商品管理、购物车、订单、任务看板、深色模式 |
| 用的是什么技术栈？ | Vue 3 + Vite + Pinia + Tailwind CSS | FastAPI + SQLite |
| 数据怎么存的？ | SQLite 数据库 4 张表 + localStorage 存主题和会话 |
| 登录怎么实现的？ | 前端发 POST → 后端查数据库 → 返回 token → 存 localStorage → 路由守卫检查 |
| 前后端怎么通信？ | Axios 发 HTTP 请求，后端 FastAPI 返回 JSON |
| 最大的难点是什么？ | (每人准备一个真实的困难 + 解决方案) |

---

## 九、答辩演示流程

### 总时长：约 15-18 分钟

```
┌──────────────────────────────────────────────────────────────┐
│ 时间线                                                        │
│                                                              │
│  0:00 ─── 组员 A 开场 ─── 项目概述 + 前端技术 (3 min)          │
│  3:00 ─── 组员 A 讲解 ─── 状态管理 + 深色模式 (5 min)          │
│  8:00 ─── 组员 A 演示 ─── 深色模式切换 / 响应式 / 登录 (3 min) │
│                                                              │
│ 11:00 ─── 组员 B 开场 ─── 系统架构 + 数据库 + API (5 min)     │
│ 16:00 ─── 组员 B 讲解 ─── 路由守卫 + 分类筛选 + 看板 (5 min)  │
│ 21:00 ─── 组员 B 演示 ─── 任务看板 CRUD + 分类筛选 (3 min)    │
│                                                              │
│ 24:00 ─── 老师提问 (5-10 min)                                 │
└──────────────────────────────────────────────────────────────┘
```

### 演示剧本

#### 组员 A 演示（3 分钟）

1. **打开登录页** → 点击右上角太阳/月亮图标切换深色模式 → 说明 localStorage 持久化
2. **调整浏览器宽度** → 展示手机/平板/桌面三端自适应
3. **用 admin 登录** → 进入控制台 → 指向侧边栏导航 → 说明路由跳转
4. **展示 Pinia DevTools** (如果装了) → 查看 user store 中的 token 和 userInfo

#### 组员 B 演示（3 分钟）

1. **在控制台切换分类下拉框** → 展示商品列表随分类变化 → 用浏览器 DevTools Network 面板展示 API 请求
2. **点击侧边栏「任务看板」** → 展示三列看板
3. **新建一个任务** → 填写标题、描述、优先级 → 创建
4. **把任务从「待处理」拖到「进行中」再「完成」** → 展示状态流转
5. **删除已完成任务** → 确认删除

---

## 十、常见 Q&A 准备

### 前端相关

#### Q1: 为什么用 Vue 3 而不是 Vue 2？
Vue 3 使用 Proxy 实现响应式（比 Vue 2 的 defineProperty 更强大），支持 Composition API（逻辑复用更方便），性能更好，Tree-shaking 更彻底。Vue 2 已于 2023 年底停止维护。

#### Q2: ref 和 reactive 有什么区别？
- `ref` 用于基本类型和对象，通过 `.value` 访问，模板中自动解包
- `reactive` 用于对象类型，直接访问属性，但不能被整体替换
- 项目中统一使用 `ref`，保持风格一致

#### Q3: computed 和 watch 的区别？
- `computed`：基于依赖**计算**一个新值，有缓存，依赖不变不重新计算
- `watch`：监听数据变化后**执行副作用**（如发请求、操作 DOM）
- 购物车总价用 computed（自动计算），分类切换用 watch（需要重新发请求）

#### Q4: 组件通信有哪几种方式？
- 父子通信：props (父→子) + emits (子→父)
- 跨组件通信：Pinia Store (本项目主要方式)
- 路由传参：query/params
- 本项目 ProductList 用 props 接收数据，用 emits 通知父组件

#### Q5: 为什么 localStorage 存 token 而不是 cookie？
对于课程设计级别的项目，localStorage 实现简单直观。实际生产环境建议用 httpOnly cookie 防 XSS 攻击。

### 后端相关

#### Q6: FastAPI 和 Flask 有什么区别？
FastAPI 是异步框架，性能更好，自动生成 OpenAPI 文档，内置 Pydantic 数据校验。Flask 更轻量但需要额外安装插件。

#### Q7: 为什么用 SQLite 而不是 MySQL？
课程设计项目数据量小，SQLite 无需安装配置，一个文件就是数据库，方便演示和迁移。

#### Q8: 跨域怎么解决的？
FastAPI 使用 CORSMiddleware 中间件，设置 `allow_origins=["*"]` 允许任何来源访问。生产环境应限制具体域名。

#### Q9: API 的 RESTful 体现在哪里？
- `GET /api/products` — 获取资源列表
- `POST /api/products` — 创建资源
- `DELETE /api/products/{id}` — 删除资源
- `PUT /api/tasks/{id}` — 更新资源
- URL 用名词复数 (products/tasks/orders)，操作由 HTTP 方法区分

#### Q10: 权限控制怎么实现的？
登录时返回的 token 格式为 `"用户名-角色"`。后端解析 token 判断角色。管理员操作（上架/下架）检查角色是否为 admin。订单查看：admin 看全部，user 只能看自己的。

### 项目设计相关

#### Q11: 项目中最大的难点是什么？（准备一个真实回答）
**建议回答方向：**
- **暗色模式适配：** Tailwind v4 的 class-based dark mode 配置方式与 v3 不同，需要查阅文档使用 `@variant` 指令
- **响应式侧边栏：** 移动端的滑动抽屉 + 遮罩层 + 点击关闭 + 路由跳转后自动关闭，状态管理比较复杂
- **任务看板：** 三列数据来自同一数据源，用 computed 分组，状态切换需要部分更新 API

#### Q12: 如果用户多了性能会不会有问题？
- 前端：Vue 虚拟 DOM + Vite 代码分割 + 路由懒加载，首屏加载快
- 后端：SQLite 并发能力有限，用户多应迁移到 PostgreSQL/MySQL
- 可优化点：商品列表分页、订单列表分页、添加缓存层

---

## 附录：关键代码对照表

### 文件速查

| 你想了解... | 看这个文件 |
|-------------|-----------|
| 怎么发 API 请求 | `src/views/Dashboard.vue` (script 部分) |
| 怎么管理全局状态 | `src/store/user.js`、`cart.js`、`theme.js` |
| 怎么做路由守卫 | `src/router/index.js` (beforeEach) |
| 怎么实现暗色模式 | `src/store/theme.js` + `src/style.css` |
| 怎么做响应式布局 | `src/App.vue` + `src/components/Sidebar.vue` |
| 怎么处理后端请求 | `main.py` |
| 数据库怎么建的 | `init_db.py` |
| 组件怎么通信 | `Dashboard.vue` ↔ `ProductList.vue` (props/emits) |
| 任务看板怎么实现 | `src/views/TaskBoard.vue` |

### 项目启动命令

```bash
# 1. 安装依赖 (首次)
npm install

# 2. 初始化数据库 (首次)
python init_db.py

# 3. 启动后端 (端口 3000)
uvicorn main:app --port 3000 --reload

# 4. 启动前端 (新开终端)
npm run dev
```

### 测试账号

| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| admin | 123456 | 管理员 | 上架/下架商品、查看全部订单 |
| zff | 123456 | 普通用户 | 浏览商品、加入购物车、结算 |

---

> 📌 **最后提示：答辩前两人一起过一遍这份文档，确保互相能回答对方的常见问题。加油！**
